[Definition]

# Inspired from https://bobcares.com/blog/fail2ban-apache-404/

failregex = ^<HOST> .* "(GET|POST|HEAD).*HTTP.*" 404 .*$
ignoreregex =.*(robots.txt|favicon.ico|jpg|png)

# Default Apache date log format
datepattern = %%d/%%b/%%Y:%%H:%%M:%%S
